{
  "name": "piggy-bank-dapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
  "start-and-deploy": "npx concurrently -k -s first \"npx hardhat node\" \"npx wait-on http://127.0.0.1:8545 && npx hardhat run scripts/deploy.js --network localhost\"",
  "auto": "node scripts/start-and-deploy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.3",
    "@nomicfoundation/hardhat-ignition": "^3.0.5",
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.1",
    "@types/chai": "^4.3.20",
    "@types/chai-as-promised": "^8.0.2",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.19.1",
    "chai": "^5.3.3",
    "dotenv": "^17.2.3",
    "ethers": "^6.15.0",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
  "hardhat": "^3.0.15",
  "concurrently": "^8.2.0",
  "wait-on": "^7.0.3",
    "mocha": "^11.7.5",
    "typescript": "~5.8.0"
  },
  "type": "module"
}
